spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://localhost:8071

logging:
  level:
    com.netfilx: WARN
    org.springframework.web: WARN
    com.optimagrowth: DEBUG